/**
 * @ems/config/pagination
 *
 * Standard pagination defaults used by API endpoints and frontend list views.
 */

export const DEFAULT_PAGE_SIZE = 50;
export const MAX_PAGE_SIZE = 500;
export const DEFAULT_PAGE = 1;
